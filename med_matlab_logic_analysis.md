# MED/MATLAB判定ロジック相違分析

## 発見された重大な問題

### 矛盾パターンの内訳
- **全50試行中32試行で矛盾（64.0%）**
- Type A (Lickあり→Miss判定): 3試行（6.0%）
- Type B (Lickなし→Hit判定): 29試行（58.0%）

### 判定結果の異常
- **HIT判定29試行のうち、全29試行（100%）で判定窓内にLickなし**
- **MISS判定21試行のうち、3試行（14.3%）で判定窓内にLickあり**

## 原因分析

### 1. MEDプログラムの判定ロジック（推測）
MEDプログラムは以下の異なる判定基準を使用している可能性：

1. **判定タイミングの相違**
   - MATLABバッチ: Go cue時刻の-1～0秒を判定窓とする
   - MEDプログラム: Go cue**後**の反応を基準にしている可能性

2. **イベントインデックスの解釈**
   - B (index_B+0): 実際には「Hit判定後のcue」かもしれない
   - H (index_B+6): 実際には「Miss判定後のcue」かもしれない
   - つまり、BとHは判定の**原因**ではなく**結果**を記録している

3. **真のGo cueの位置**
   - E (index_B+3): "event1, CSp reward" - これが実際のGo cueの可能性
   - または別のイベントタイプがGo cueを表している

### 2. データ構造の解釈ミス

現在のMATLABバッチの問題：
```matlab
Hit_cue = Hit_cue - CS_time;  % B配列から1秒引く
miss_cue = miss_cue - CS_time; % H配列から1秒引く
```

この処理は以下を前提としている：
- B/Hイベントの1秒前にGo cueがある
- その-1～0秒の間のLickで判定が決まる

しかし実際は：
- B/Hは判定**結果**を記録したマーカー
- 真のGo cueは別の場所にある

## エラー発生条件

### Type B エラー（58.0%）
- **全てのHIT判定で発生**
- MEDプログラムは何か別の基準でHIT判定している
- 可能性：
  1. Go cue後の特定時間窓でのLick
  2. 報酬予測に基づく判定
  3. 別のセンサーやトリガーの使用

### Type A エラー（6.0%）
- 3試行のみで発生
- これらの試行では実際に-1～0秒にLickがあったが、MEDはMissと判定
- 可能性：
  1. Lickの持続時間が不足
  2. 特定の閾値を満たさない
  3. 判定窓のタイミングのずれ

## 修正案

### 案1: 真のGo cueイベントを特定
1. Eイベント（event1）を基準に解析し直す
2. 各イベントタイプの時系列関係を詳細に分析
3. MEDプログラムのソースコードを確認

### 案2: 判定窓の調整
1. Go cue後の0～1秒を判定窓とする
2. 複数の時間窓で解析を試行
3. 最も矛盾の少ない窓を採用

### 案3: イベント定義の再確認
1. MEDプログラムの仕様書を確認
2. 各イベントコードの正確な意味を把握
3. MATLABバッチを正しい解釈に基づいて修正

## 推奨アクション
1. **緊急**: MEDプログラムの仕様書・ソースコードの確認
2. **即時**: Eイベント（event1）を基準とした再解析
3. **検証**: 他の日付のデータでも同様のパターンが見られるか確認