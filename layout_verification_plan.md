# 2×2グリッドレイアウト動作検証計画書

## 検証概要
本計画書は、managerウィンドウの2×2グリッドレイアウト実装後の動作検証について定義します。

## 検証対象スクリプト
- `setup-manager-grid-layout.sh` - 2×2グリッドレイアウト設定スクリプト

## 起動パターン別検証計画

### 1. 標準起動パターン

#### A. quick-start.sh経由
```bash
# 検証手順
1. ./quick-start.sh
2. ./setup-manager-grid-layout.sh
3. レイアウト確認
```
**検証項目:**
- [ ] セッション"multiagent"が正常に作成されるか
- [ ] managerウィンドウが存在するか
- [ ] 2×2グリッドレイアウトが正常に適用されるか
- [ ] 各ペインのラベルが正しく表示されるか

#### B. start-system-auto.sh直接起動
```bash
# 検証手順
1. ./start-system-auto.sh
2. ./setup-manager-grid-layout.sh
3. レイアウト確認
```
**検証項目:**
- [ ] 自動初期化後にレイアウト変更が可能か
- [ ] エージェントへの初期化メッセージが影響しないか
- [ ] レイアウト変更後もエージェントが正常動作するか

#### C. start-system.sh標準起動
```bash
# 検証手順
1. ./start-system.sh
2. ./setup-manager-grid-layout.sh
3. レイアウト確認
```
**検証項目:**
- [ ] CEOセッションとteamセッションが正常に作成されるか
- [ ] teamセッション（4分割）は影響を受けないか
- [ ] managerウィンドウのみ変更されるか

### 2. 特殊起動パターン

#### D. start-system-staggered.sh（段階的起動）
```bash
# 検証手順
1. ./start-system-staggered.sh
2. 全エージェント起動完了待機
3. ./setup-manager-grid-layout.sh
4. レイアウト確認
```
**検証項目:**
- [ ] 段階的起動中のレイアウト変更は避けるべきか
- [ ] 起動完了後のレイアウト変更は正常か
- [ ] API負荷軽減の効果は維持されるか

#### E. start-system-d-drive.sh（Dドライブ環境）
```bash
# 検証手順
1. ./start-system-d-drive.sh
2. ./setup-manager-grid-layout.sh
3. レイアウト確認
```
**検証項目:**
- [ ] セッション名"ai-team"での動作確認
- [ ] スクリプトの修正が必要か（セッション名の不一致）
- [ ] 5ウィンドウ構成での影響確認

#### F. workflow-launcher.sh（プロジェクトベース）
```bash
# 検証手順
1. ./workflow-launcher.sh
2. プロジェクト選択
3. ./setup-manager-grid-layout.sh
4. レイアウト確認
```
**検証項目:**
- [ ] プロジェクト別セッション名での動作確認
- [ ] モニタリングセッションへの影響確認
- [ ] 複数プロジェクト環境での検証

### 3. 既存セッション再利用パターン

#### G. 既存セッションへの適用
```bash
# 検証手順
1. tmuxセッションが既に存在する状態
2. ./setup-manager-grid-layout.sh
3. レイアウト確認
```
**検証項目:**
- [ ] 既存ペインのクリーンアップが正常か
- [ ] 作業中のデータ消失リスクの確認
- [ ] 警告メッセージの表示確認

#### H. レイアウト再適用
```bash
# 検証手順
1. ./setup-manager-grid-layout.sh（1回目）
2. ./setup-manager-grid-layout.sh（2回目）
3. レイアウト確認
```
**検証項目:**
- [ ] 既存の2×2レイアウトの再構築が正常か
- [ ] エラーなく実行されるか
- [ ] ペイン番号の一貫性確認

### 4. エラーケース検証

#### I. セッション未作成時
```bash
# 検証手順
1. tmuxセッションが存在しない状態
2. ./setup-manager-grid-layout.sh
3. エラーメッセージ確認
```
**検証項目:**
- [ ] 適切なエラーメッセージが表示されるか
- [ ] 終了コードが正しく設定されるか

#### J. managerウィンドウ非存在時
```bash
# 検証手順
1. managerウィンドウを手動で削除
2. ./setup-manager-grid-layout.sh
3. エラーメッセージ確認
```
**検証項目:**
- [ ] ウィンドウ不在のエラー検出が正常か
- [ ] 適切な対処方法が案内されるか

## 検証環境要件
- tmux 2.3以上
- bash 4.0以上
- 各起動スクリプトが正常動作すること

## 検証成功基準
1. すべての起動パターンで2×2グリッドレイアウトが正常に適用される
2. 既存のエージェント動作に影響を与えない
3. エラーケースで適切なメッセージが表示される
4. レイアウト変更後も全機能が正常動作する

## 推奨事項
1. **レイアウト適用タイミング**
   - システム起動完了後に適用を推奨
   - 自動化する場合は`start-system-auto.sh`への統合を検討

2. **セッション名の統一**
   - 現在の実装は"multiagent"セッション専用
   - 他のセッション名に対応する場合はパラメータ化を検討

3. **バックアップ機能**
   - 重要な作業中のレイアウト変更を避ける警告追加
   - 現在のレイアウト状態を保存する機能の検討

## 検証スケジュール案
1. 基本動作検証（標準起動パターン）: 30分
2. 特殊起動パターン検証: 45分
3. エラーケース検証: 15分
4. 統合テスト: 30分

合計所要時間: 約2時間